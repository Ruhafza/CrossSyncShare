// TEMPLATE ONLY — this file is committed to git and contains no real values.
//
// js/firebase-config.js (the file the app actually imports) is generated
// automatically from environment variables by scripts/generate-firebase-config.js
// and is gitignored, so your real project config never gets committed.
//
// For quick local testing without the generator script, you can copy this
// file to js/firebase-config.js and fill in real values by hand instead —
// see README.md for both options.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
